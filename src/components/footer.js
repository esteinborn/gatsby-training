import React from "react"

const Footer = ({copyDate}) => (
  <footer>
    © {copyDate}, Served and built with
    {` `}
    <a href="https://www.gatsbyjs.org">Gatsby</a>
  </footer>
)

export default Footer
